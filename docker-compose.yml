services:
  app:
    build: .
    container_name: ecommerce_app
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - sqlite_data:/app/instance
    env_file:
      - .env

volumes:
  sqlite_data:
